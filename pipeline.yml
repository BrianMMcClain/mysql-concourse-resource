resource_types:
- name: mysql
  type: docker-image
  source:
    repository: brianmmcclain/mysql-concourse-resource
    tag: 0.0.1
resources:
- name: mysql-gcp
  type: mysql
  source:
        host: ((host))
        user: ((user))
        password: ((password))
        database: ((database))
        table: ((table))

jobs:
- name: hello-world
  plan:
  - get: mysql-gcp
    trigger: true
  - task: show-result
    config:
      inputs: 
      - name: mysql-gcp
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: ubuntu}
      run:
        path: cat 
        args: ["./mysql-gcp/concourse-mysql-resource.XXXXXX"]
  - put: mysql-gcp
    params:
      column: processed
      value: true